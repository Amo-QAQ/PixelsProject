# 2026-08-24

## 资产包整理（itch 发售准备）

- 完成资产包全量盘点：96 个 PNG，无 Aseprite 源文件，基础网格 16px，Animals/Chick 帧为 21x20，Characters 帧为 48x96。
- 用户决策：方向命名统一为小写 `_f/_b/_l/_r` 后缀（不用 N/E/S/W）；其余命名问题（拼写、大小写、顺序）逐条确认后再改。
- 已执行：批量重命名 48 个文件（方向标识统一为小写 f/b/l/r 并移至文件名末尾）。映射表存于 `.workbuddy/rename_direction_map.csv`（可回滚）。
- 待确认清单（未处理）：
  1. 拼写：`ideal_girl`→`idle_girl`（8 文件）、`Grass_Lsland`→`Grass_Island`（2 文件）
  2. 文件名空格：`Doors and windows_Tiles.png`
  3. PascalCase vs snake_case 统一（House_01、Berry_Crop、Grass_Tiles 等）
  4. 词序统一：chick 系存在 `chick_walk_b` 与 `walk_chick_b` 两种顺序并存；girl 系为 `action_subject_dir`。拟统一为 `subject_action_dir`
  5. chick 缺 R 方向（仅 b/f/l 三向，girl 为四向）
  6. `chick_walk_b` 与 `walk_chick_b` 两个 walk 文件内容不同但命名易混，需确认是否保留其一
  7. `Terrain/Resources/tree.png` 与 `Props/Tree_Props.png` 树的概念重复
  8. 目录结构是否加 Sprites/ 统一层 —— 用户待定
- read_me 方案：用户表示将拆分免费版/商业版两个包发售；需重写为 LICENSE + README + CHANGELOG 三件套，双版本共用主目录、发布时按清单打包两个 zip。

## 第二批整理（用户确认后执行）

- 删除 Chick 冗余 walk 动画：`walk_chick_b/f/l` 与 `chick_walk_b/f/l` 中 b 方向像素完全相同、用户确认 walk_chick 系为重复导出，已移至 `.workbuddy/trash/`。
- 批量重命名 88 个文件：拼写修正（ideal→idle, Lsland→Island）、全小写 snake_case、去除空格、CamelCase 拆分（RoofA→roof_a）、角色类统一词序为 `subject_action_dir`（如 `girl_ax_b`, `chick_eat_b`）。映射表存于 `.workbuddy/rename_full_map.csv`。
- 生成双版本许可文件：`LICENSE_FREE.txt`（禁止商用/转售）与 `LICENSE_COMMERCIAL.txt`（允许商用/禁止转售原始文件）。
- 命名规范最终确定：角色类 = `角色_动作_方向`（如 `girl_walk_f`, `chick_idle_1_b`）；其他资产 = `资产名_分类_属性`（如 `grass_tiles`, `berry_crop`, `flower_a_props`）。

## 第三批：文档完善

- 用户反馈 LICENSE 全大写标题不友好，已重写为中英双语、正常大小写（Sentence case）、You MAY / may NOT 清单式版本。
- 重写说明文档：新建 `README.md`（英文为主+中文简介，含内容清单表、16px 规格、命名规范说明、双版本许可指引、占位符待填）。旧 `read_me.txt` 移入 `.workbuddy/trash/read_me_old.txt`。
- 当前资产总数：92 个 PNG（96 - 3 冗余）。README 与 LICENSE 均有 `[Your Name / Studio]` 等占位符待用户替换。

## 第四批：删除类型尾缀

- 用户要求删除文件名末尾的类型尾缀（`_tiles`/`_props`/`_crop`），共重命名 18 个文件：`grass_tiles→grass`、`berry_crop→berry`、`roof_a_tiles→roof_a`、`doors_windows_tiles→doors_windows` 等。映射表存于 `.workbuddy/rename_remove_suffix_map.csv`。
- 命名规范更新：其他资产 = `资产名_属性`（如 `grass`, `grass_shadow`, `berry`, `house_01`），资产类型由所在目录表达（Terrain/Tilesets/ 下即为图块集）。
- README.md 命名规范章节已同步更新。

## 第五批：chicken 改名

- 用户要求 `chick` 全部改为 `chicken`，编号格式 `_1/_2` 改为 `01/02`：重命名 21 个文件（如 `chick_idle_1_b` → `chicken_idle01_b`），目录 `Animals/Chick` → `Animals/Chicken`。映射表存于 `.workbuddy/rename_chicken_map.csv`。
- README 中 chick 相关引用（目录说明、规格、命名示例）已同步更新。

## 第六批：Chicken 方位顺序修正

- 用户重新导出了 Chicken 动画（命名变为 `chicken_b_eat` 方向在中间），要求方向移到末尾。已重命名 21 个文件回 `chicken_eat_b` 格式。映射表存于 `.workbuddy/rename_chicken_dir_last_map.csv`。
- 注意：用户会从 Aseprite 重新导出文件覆盖目录，后续整理前先 `ls` 确认实际状态。
