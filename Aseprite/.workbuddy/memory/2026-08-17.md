# 2026-08-17 工作日志

## 精灵图合成工具重构
- **问题**: DearPyGui GUI版EXE反复报错（`add_same_line`、`borders_inner`等API兼容性问题）
- **决策**: 用户要求放弃GUI/EXE方案，改用纯Python命令行交互脚本
- **结果**: 重写 `合成精灵图.py` v2.0 — 纯命令行交互式，双击运行
  - 三步引导：输入路径 → 输出路径 → 预览确认
  - 支持拖拽文件夹到窗口（自动去引号）
  - 显示完整分组预览表（组名/帧数/预计尺寸）
  - 可选横排/纵排 + 帧间距
  - 合成后显示输出文件列表，可选打开目录

## 清理文件
删除了以下EXE打包相关垃圾：
- 精灵图合成工具.py (DearPyGui源码)
- 精灵图合成工具.spec
- 精灵图合成工具v2.exe (16MB)
- 运行合成工具.bat

## 当前工具链
1. Aseprite CLI 导出单帧: `aseprite -b xxx.aseprite --split-tags --split-slices --save-as output/{tag}{slice}.png`
2. Python 合成精灵图: 双击 `运行合成工具.bat`

## Bug修复
- **Pillow未安装**: 双击闪退，已安装到系统Python 3.12 + 管理 Python 3.13；修复自动安装逻辑用 `sys.executable`
- **bat找不到python**: 系统PATH无python命令，改用完整路径 `C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe`
- **bat编码问题**: `%` 符号被吃掉，改用PowerShell以GBK编码写入
- **组名末尾多余下划线**: 正则从 `^(.*?)(-?)(\d+)$` 改为 `^(.*?)[_-](\d+)$`，支持 `_` 和 `-` 分隔符
- **去掉JSON输出**: 用户不需要JSON数据文件，已移除

## 英文版发布
- 生成位置: `D:\Desktop\新建文件夹\sprite_sheet_combiner.py` + `run.bat`
- 默认路径: 输入 `export/`, 输出 `sprite_sheets/`（符合国际习惯）
- 全英文界面提示，可分享给外国用户
