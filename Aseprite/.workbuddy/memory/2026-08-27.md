# 2026-08-27

- 修复精灵图拆分工具 `sprite-splitter.html` 在浏览器内置预览中因 `file://` 协议触发安全限制的问题
- 通过 Python 启动本地 HTTP 服务器（端口 8080）访问工具，File System Access API 可正常工作
- 工具访问地址：http://localhost:8080/sprite-splitter.html
- 修复因脚本用 IIFE 包裹导致 inline onclick 找不到 `autoSplitAll` 等函数的问题，将所有按钮函数显式挂载到 `window`
- 修改自动命名规则：取消 A/B/C 字母分组，改为以导入图片的文件名（去掉扩展名）作为前缀，例如 `berry_01.png`、`cabbage_02.png`
- 修复 `applyAll()` 中残留的 `getLetter` 调用（已删除该函数）
- 添加预览区缩放功能：默认 2 倍放大，支持 1x/2x/3x/4x 切换，方便框选操作
- 修复导出文件名与预览不一致的问题：自动拆分/手动框选时直接设置默认名（如 `berry_01`），不再留空导致 fallback 成 `_unnamed`
- 修复 Canvas `willReadFrequently` 警告：hidden canvas getContext 加 `{ willReadFrequently: true }` 选项
- 新增右键直接删除卡片（无确认弹窗）
- 新增卡片拖拽排序（HTML5 Drag & Drop API，同图内拖拽重排）
- 新增拖拽/删除后自动重编号功能（`renumberAreas`，保留用户手动改过的名字）
- 修复导出文件名重复问题（如 `onion_01_1.png`）：renumberAreas 改用正则 `^前缀_\d+$` 精确匹配，导出前强制重编号一次
- 修复单击添加区域时 `c1 is not defined` 报错：变量名写错（c1/c2/r1/r2 → c/c/r/r）
- 修复卡片输入框名称与 meta 显示不一致：renderList 开头先对全部图片调 renumberAreas 同步 name，卡片和 meta 统一用 defaultName 显示
- 新增完整命名系统：全局前缀/后缀 + 原文件名或自定义名称 + 自动序号，格式为 `前缀_名称_后缀_01`
- 新增命名预览：实时显示生成的文件名示例
- 升级批量重命名：支持应用规则或查找替换模式
- 修复坐标显示：不再对 overlay 整体 scale，改为按 zoom 单独计算每个坐标像素位置，文字大小固定不随缩放变化，坐标居中显示
- 重构网格设置布局：预设按钮改为横排紧凑排列（含像素游戏常用尺寸：8×8/16×16/24×24/32×32/48×96角色/21×20鸡/64×64），去掉无用的"应用到全部图片"按钮，新增图片尺寸提示
- 重置按钮新增「清除手动标记」勾选框（默认勾选），用户可选择重置时是否清除手动命名的标记
